import React from 'react';
//eslint-disable-next-line no-unused-vars
import validate from "@workpal-build/jwt-token-validation";

// Default implementation, that you can customize
export default function Root({children}) {
  return <>{children}</>;
}
